[rules]
    scan-rules = [
        "AWS.S3Bucket.DS.High.1043",
        "tenable.kubernetes.IAM.107"
    ]
    skip-rules = [
        "AWS.S3Bucket.IAM.High.0370",
        "tenable.kubernetes.IAM.5",
        "tenable.kubernetes.OPS.461",
        "tenable.kubernetes.IAM.109"
    ]

[severity]
level = "low"

[category]
list = [
    "RESILIENCE",
    "IDENTITY AND ACCESS MANAGEMENT",
]
