notifications:
  webhook:
    type: webhook
    config:
      url: 'https://httpbin.org/post'
  webhook1:
    type: webhook
    config:
      url: 'https://httpbin.org/post'
rules:
  scan-rules:
    - AWS.S3Bucket.DS.High.1043
    - AWS.S3Bucket.IAM.High.0370
  skip-rules:
    - AWS.S3Bucket.DS.High.1043
    - AWS.S3Bucket.IAM.High.0370
